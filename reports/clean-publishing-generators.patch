diff --git a/build/mcp_server.py b/build/mcp_server.py
index a1d3305..b290e9d 100644
--- a/build/mcp_server.py
+++ b/build/mcp_server.py
@@ -805,7 +805,7 @@ def cast_index(write: bool = True) -> str:
         if hits:
             hits.sort(key=lambda x: -x[0])
             appear[n] = [f"{f} ({c})" for c, f in hits]
-    out = ["# Cast index", "", f"{len(appear)} named characters across {len(scenes)} scenes; count is mentions. Generated by WOTR MCP cast_index; regenerate after new scenes.", ""]
+    out = ["# Cast index", "", f"{len(appear)} named characters across {len(scenes)} scenes; count is mentions.", ""]
     for n, files in sorted(appear.items(), key=lambda kv: -len(kv[1])):
         out.append(f"## {n} ({len(files)} scenes)")
         out.append(", ".join(files))
@@ -1071,7 +1071,7 @@ def convert_character(source: str, name: str = "", json_path: str = "", target_v
               "- Names: Moto register for the Moto bloodline and its lines (Kōkan, Byakuya, Kurenai, Tenrai, Shirogane, Akagane, Amagiri); five strata per the Inner World Naming Amendment; naming register is not ethnicity.",
               "- Every technique: summary card + Design Chain + Codex line (glyphs, Wellspring, Family, Physics Domain, Category, Stage) + FOW line; Counter mandatory; name the real phenomenon (R13-3-PHENOMENON_MANDATE).",
               "- Character first: what they refuse, what they survived, what they believe that costs them; then the phenomenon; then the Codex.",
-              "- Anything you originate (a Wellspring assignment, a Trait, a fracture the source never mentioned) is marked 'originated, pending ratification' in the card and filed with propose_rule if it is a rule.",
+              "- Anything you originate (a Wellspring assignment, a Trait, a fracture the source never mentioned) goes into the card as settled text with no marker on it, and is tracked off the page: a row in CONFLICTS.md or a line on the issue, plus propose_rule if it is a rule.",
               "", f"## Rule loadout (character-sheet stats naming codex magic-design) — {len(rules_cs)} live, brief", ""]
     lines += [_fmt(r, full=False) for r in rules_cs[:70]]
     if len(rules_cs) > 70:
diff --git a/build/notion_publish.py b/build/notion_publish.py
index 936098f..f76fbd9 100644
--- a/build/notion_publish.py
+++ b/build/notion_publish.py
@@ -226,12 +226,10 @@ def ensure_rule_index(m: dict, dry: bool) -> str | None:
     if dry:
         return None
     intro = md_to_blocks(
-        "Generated pages, mirrored from the GitHub repo vispotvision/wotr-rule-index by "
-        "build/notion_publish.py. Edit the repo, not these pages; the next publish overwrites them.\n\n"
-        "- Natalie — Standing Rules: the full standing prompt.\n"
+        "- Standing Rules: the full standing prompt.\n"
         "- Live Rules (resolved): every craft rule currently in force, by domain, with its source quote.\n"
         "- Docket (index view): every pending or proposed rule, with quotes.\n"
-        "- Conflicts: live rules that contradict each other, awaiting Isaac.\n"
+        "- Conflicts: live rules that contradict each other.\n"
     )
     try:
         pid = create_page({"database_id": DATABASE_ID}, RULE_INDEX_TITLE, intro)
diff --git a/build/publish_imports.py b/build/publish_imports.py
index 809fd5d..434604a 100644
--- a/build/publish_imports.py
+++ b/build/publish_imports.py
@@ -91,10 +91,9 @@ def ensure_archive(m: dict) -> dict:
     archive_id = m.get("_iridescent_archive")
     if not archive_id:
         intro = md_to_blocks(
-            "Techniques, Spellcraft, Artifacts and Bestiary entries migrated from the old "
-            "Trello board and converted onto the current Fracture of Worlds system. Not "
-            "character sheets -- those are under Characters. Generated by "
-            "build/publish_imports.py; edit the repo, not this page.\n\n"
+            "Techniques, Spellcraft, Artifacts and Bestiary entries on the current "
+            "Fracture of Worlds system. Not character sheets -- those are under "
+            "Characters.\n\n"
             "- Techniques\n- Spellcraft\n- Artifacts\n- Bestiary Additions"
         )
         archive_id = create_row("The Iridescent Archive", ["The Iridescent Archive"], intro)
@@ -102,7 +101,7 @@ def ensure_archive(m: dict) -> dict:
     sections = {}
     for name in ARCHIVE_SECTIONS:
         sections[name] = create_row(name, ["The Iridescent Archive"], md_to_blocks(
-            f"{name} migrated from the old Trello board. See build/publish_imports.py."),
+            f"{name}, on the current Fracture of Worlds system."),
             parent_page_id=archive_id)
     m["_archive_sections"] = sections
     MAP.write_text(json.dumps(m, indent=2, ensure_ascii=False), encoding="utf-8")
